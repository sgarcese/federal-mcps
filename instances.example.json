{
  "$comment": [
    "EXAMPLE fleet record (ADR-004). Copy to instances.json (gitignored) and fill in your own",
    "AWS account, region, domain and Terraform state bucket. The real instances.json is never",
    "committed; instance.mjs and terraform/instances/dev/locals.tf fall back to this example when",
    "it is absent (so CI \u2014 which uses -backend=false + a mocked provider \u2014 runs on placeholders).",
    "domain.aliases.<service> lists extra hostnames served by the same API (ADR-016 \u00a72); the short <service>.responsive.city names live here while the *-mcp names stay the primary domain.",
    "cdcDomainName / cdcService: the OpenContext portal Lambda for data.cdc.gov (ADR-016 \u00a75), serving the CDC PLACES source guide.",
    "hudDomainName / hudService: the HUD User server (M11, ADR-018); its primary hostname is already the short hud-user.responsive.city name."
  ],
  "instances": [
    {
      "name": "dev",
      "description": "federal-mcps dev in the Responsive City account",
      "account": "123456789012",
      "region": "us-east-1",
      "environmentTag": "dev",
      "domain": {
        "blsDomainName": "bls-mcp.responsive.city",
        "geoDomainName": "geo-mcp.responsive.city",
        "hostedZoneId": "Z02890412WHZ405FIOT0Y",
        "hostedZoneName": "responsive.city",
        "censusDomainName": "census-mcp.responsive.city",
        "aliases": {
          "bls": ["bls.responsive.city"],
          "geo": ["geo.responsive.city"],
          "census": ["census.responsive.city"]
        },
        "cdcDomainName": "cdc.responsive.city",
        "hudDomainName": "hud-user.responsive.city"
      },
      "terraform": {
        "stateBucket": "rc-tfstate-123456789012",
        "stateKey": "rc/federal-mcps/dev/terraform.tfstate"
      },
      "naming": {
        "blsService": "rc-bls-mcp",
        "geoService": "rc-geo-mcp",
        "censusService": "rc-census-mcp",
        "cdcService": "rc-cdc-mcp",
        "hudService": "rc-huduser-mcp"
      }
    }
  ]
}
