# Copy to .env for local development. Never commit .env.
# In deployed environments these come from AWS Secrets Manager, not env files.

# Bureau of Labor Statistics Public Data API v2 (free registration, 500 queries/day)
# https://data.bls.gov/registrationEngine/
BLS_API_KEY=

# Census Data API (free registration)
# https://api.census.gov/data/key_signup.html
CENSUS_API_KEY=

# HUD User Data API token (free registration; required for every data query once an
# indicator tool lands, M11+): register at https://www.huduser.gov, then generate a
# token under your account's API access settings.
HUD_USER_TOKEN=

# Socrata app token for data.cdc.gov (optional; raises throttling limits)
# https://data.cdc.gov/profile/edit/developer_settings
SOCRATA_APP_TOKEN=

# Local transport: "stdio" for MCP clients that launch the process, "http" for a local server
MCP_TRANSPORT=stdio
MCP_HTTP_PORT=3000

# Set to 1 to run live smoke tests against real agency APIs (off by default; CI uses fixtures)
LIVE_TESTS=0

# Optional: a Socrata App Token for the CDC portal Lambda (ADR-016 §3). data.cdc.gov serves
# untokened requests; a token only lifts per-IP throttling. Leave empty to send none.
SOCRATA_APP_TOKEN=
