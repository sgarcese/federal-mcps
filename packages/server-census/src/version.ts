/**
 * The package version, reported in `initialize` and by the auto-registered
 * `census_describe_source` tool. Kept in sync with `package.json` by hand.
 */
export const CENSUS_SERVER_VERSION = "0.5.0";
